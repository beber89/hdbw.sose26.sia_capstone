version: "3.8"

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    volumes:
      - .:/workspace
    ports:
      - "8080:8080"
    environment:
      - GO111MODULE=on
    command: ["go", "run", "."]
