## Tasks

- [X] Add to setup: mqtt simulator that utilize random measurements.
- [X] Mqtt closed circuit: publisher (simulator), client (proxmox), broker (vps).
- [ ] Issue Report: Any device can publish fake information.
  - [ ] Fix: Add authentication
- [ ] Issue Report: No Zeroization for private key stored in devices
  - [ ] Fix: Hardware Zeroization
  - [ ] Fix: Apply a key exchange algorithm that receives creates new keys based on a secret seed stored centrally
- [ ] Issue Report: Fake broker Or Man in the middle 
  - [ ] Fix: Get TLS

## Commands

```

docker run --rm \
  -v "$(pwd)/mqtt-mocker/settings.generated.json:/usr/src/app/settings.json:ro" \
  mqtt-simulator:latest \
  -f /usr/src/app/settings.json

docker run --rm eclipse-mosquitto:latest  mosquitto_sub -h $BROKER_URL -p 1883 -t "place/#" -v
```
```
```
